#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {

	// variavel
	
	int n, cont=1, par=0,soma=0;
	
	// desenvolvimento
	
	printf (" Digite um numero para a soma dos pares: ");
	scanf  ("%d",&n);
	
 
	 while(cont <= n ){
 		printf("%d ", par);
 		if (par = par + 2){
 		 soma = soma + n;
 	}
 		 cont++;
 }
 printf ("\n %d", soma);
    return 0;	
 }

